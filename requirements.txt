# The host ComfyUI environment gets comfy-env and NOTHING else.
# Every real dependency -- torch, diffusers, xformers, the native CUDA
# extensions -- is declared in nodes/comfy-env.toml and installed into this
# pack's own isolated environment by install.py.
comfy-env==0.4.22
